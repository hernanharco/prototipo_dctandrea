# backend

API del proyecto. Pendiente de definición de stack y alcance.

Estado actual: los formularios del frontend (`Booking`, `ChatWidget`) simulan la
llamada a la API con `setTimeout`. Cuando se defina el backend real, se reemplaza
esa simulación por llamadas HTTP reales.